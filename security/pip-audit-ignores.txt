# Vulnerability IDs pip-audit may ignore in CI — one per line, with the reason.
#
# Format:   <ID>   # <why it does not apply or cannot be fixed>; added YYYY-MM-DD; re-check YYYY-MM-DD
#
# Rules (docs/audits/2026-09-13-security-architecture-audit.md, Phase 5):
#   * A finding WITH a fixed release is never ignored — bump the dependency floor instead.
#   * A finding with NO fixed release is ignored here with a re-check date, and removed the
#     moment a fix ships.
#   * The CI step reads this file verbatim; an entry without a reason is a review finding.
#
# This file is ClaudIA's own, deliberately, even where a line also appears in
# ibkr_core_mcp/security/pip-audit-ignores.txt. Each repo accepts its own risk: reading the
# other repo's file would let a line removed there break this gate with no commit here, and
# would hide the day the two trees stop agreeing about what is acceptable.

PYSEC-2026-3740   # nltk <= 3.10.3 model-artifact path traversal (TransitionParser/AveragedPerceptron/PerceptronTagger/maxent); NO fixed release; reaches this tree only through ibkr_core_mcp's [scraper] extra -> crawl4ai, which uses nltk for tokenising and calls none of those APIs; ClaudIA itself never imports nltk; added 2026-09-14; re-check 2026-10-13
